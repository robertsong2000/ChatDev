# OpenAI API Configuration
# 必填：OpenAI API密钥
OPENAI_API_KEY=openai_api_key

# 可选：自定义API端点（用于OpenAI兼容的API服务）
# 例如：https://api.openai.com/v1 或其他兼容服务的URL
BASE_URL=https://ark.cn-beijing.volces.com/api/v3

# 可选：默认模型名称
# 内置支持的OpenAI模型：GPT_3_5_TURBO, GPT_4, GPT_4_TURBO, GPT_4O, GPT_4O_MINI
# 自定义模型：支持任何OpenAI兼容的API模型，直接填写模型名称即可
# 示例：gpt-4, gpt-3.5-turbo, doubao-seed-1-6-250615, claude-3-sonnet 等
DEFAULT_MODEL=doubao-seed-1-6-250615

# 可选：模型参数配置
# 温度参数，控制输出的随机性 (0.0-2.0)
TEMPERATURE=0.2

# Top-p参数，控制输出的多样性 (0.0-1.0)
TOP_P=0.6

# 最大重试次数
MAX_RETRIES=5

# 示例配置说明：
# 1. 将 your_openai_api_key_here 替换为您的实际API密钥
# 2. 如果使用其他OpenAI兼容的API服务，请取消注释并设置BASE_URL
# 3. 根据需要调整模型参数